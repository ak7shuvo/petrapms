// No body required — issuing an invoice is a state transition only.
// Kept as an empty DTO so the endpoint is consistent with the rest of
// the API's pattern (all mutations go through typed DTOs).
export class IssueInvoiceDto {}
